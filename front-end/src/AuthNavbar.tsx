import React from 'react';
import Auth from './Auth';

const NavbarAuth = function () {
    return (
        <div className="navbar">
            <span> chat_app </span>
            <Auth />
        </div>
    );
};

export default NavbarAuth;
